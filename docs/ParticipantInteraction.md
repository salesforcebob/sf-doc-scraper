# ParticipantInteraction

## Overview

Interaction types for participant views.

## Declaration

```swift
enum ParticipantInteraction
```

## Enumeration Cases

| Case | Signature | Description |
| --- | --- | --- |
| `directMessage` | `directMessage(String)` | Send a direct message to a participant |
| `mute` | `mute(String)` | Mute a participant |
| `viewProfile` | `viewProfile(String)` | View a participant's profile |